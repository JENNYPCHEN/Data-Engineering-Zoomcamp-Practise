{
  "name": "My Codespace",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "hostRequirements": {
    "cpus": 4
  },
  "waitFor": "onCreateCommand",
  "updateContentCommand": "python3 -m pip install -r requirements.txt && curl -fsSL https://apt.releases.hashicorp.com/gpg | apt-key add - && apt-add-repository 'deb [arch=amd64] https://apt.releases.hashicorp.com focal main' && apt-get update && apt-get install terraform",
  "postCreateCommand": "",
  "dockerComposeFile": "docker-compose.yml",
  "service": "taxi_ingest",
  "workspaceFolder": "/workspace",
  "forwardPorts": [5432, 8080],
  "customizations": {
    "codespaces": {
      "openFiles": []
    },
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter",
        "ms-python.python"
      ]
    }
  },
  "runArgs": ["--volume=/var/lib/docker", "--privileged"]
}
